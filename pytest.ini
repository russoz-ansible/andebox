# Pytest configuration for coverage and JUnit XML reporting
[pytest]
addopts =
    --cov=andeboxlib --cov-report=term-missing
    --cov-report=html:htmlcov
    --junitxml=junit.xml

testpaths =
    tests
